# Domain Management Plane (`<domain>-mgmt/`) — DPI Center

This directory contains the central Infrastructure-as-Code (IaC), identity configurations, and operational governance tools for the **sandbox** Sovereign Domain under Google Cloud Organization **`dpi.ait.ac.th`** (`350922776586`).

---

## 🏛️ Architecture Overview

The Sovereign Domain management plane is designed to be:
* **100% Stateless & GitOps Managed**: All infrastructure state is defined declaratively in Terraform and version-controlled.
* **Cost-Efficient**: Operates at ~$0.20/month base cost (covering only authoritative Cloud DNS records and GCS state storage).
* **Decoupled & Sovereign**: Owns its own GCS remote state bucket (`gs://sandbox-dpi-ait-ac-th-tfstate`), Cloud DNS subzone (`sandbox.dpi.ait.ac.th`), and Secret Manager vault.
* **Zero Blast Radius**: Workload projects under this domain declare project containers in `prj-*.tf` envelopes without impacting the base platform or other sovereign domains.

```text
sandbox-mgmt/
├── README.md                      # Domain management plane runbook (this file)
├── oauth_setup.md                 # Google OAuth2 / OIDC console setup SOP
└── terraform/                     # Domain Cloud DNS, IAM, Secrets, Project Envelopes
    ├── main.tf                    # GCS backend & project protection lien
    ├── dns.tf                     # Authoritative Cloud DNS subzone
    ├── iam.tf                     # Folder-level IAM bindings
    ├── secrets.tf                 # Secret Manager baseline & billing resolution
    ├── variables.tf               # Terraform input variables
    ├── outputs.tf                 # Domain outputs (nameservers, DNS)
    ├── prj-workload.tf.example    # Starter template for workload project envelopes
    └── terraform.tfvars.example   # Sample variables configuration
```

---

## 🚀 Quick Deployment Guide

1. **Verify `terraform.tfvars`**:
   Ensure `terraform.tfvars` is present (automatically generated by `scripts/bootstrap_domain.sh`).

2. **Initialize Terraform Backend**:
   ```bash
   cd terraform
   terraform init
   ```

3. **Deploy Management Plane**:
   ```bash
   terraform plan
   terraform apply
   ```

4. **Complete DNS Delegation Handshake**:
   Capture the assigned nameservers from `terraform output name_servers` and add them as an NS record in parent zone `dpi-center` (`ait-brainlab-mgmt`). See `docs/sop-domain-mgmt.md` for complete runbook.
